'use client'

import { useMemo, useState } from 'react'
import {
  ArrowUpRight, Bell, Bot, Check, ChevronRight, CircleDollarSign, Home,
  ListChecks, MapPin, Moon, Plus, Search, ShoppingCart, Sparkles, Sun,
  Tag, TrendingDown, Users, Wallet, X,
} from 'lucide-react'

type Tab = 'Domů' | 'Nákup' | 'Obchody' | 'Rozpočet' | 'AI' | 'Profil'
type Item = { id: number; name: string; detail: string; price: number; quantity: number; done: boolean; color: string }

const initialItems: Item[] = [
  { id: 1, name: 'Mléko polotučné', detail: '2 l · Mlékárna Kunín', price: 44.9, quantity: 2, done: false, color: 'bg-sky-100 text-sky-700' },
  { id: 2, name: 'Banány', detail: '1 kg · volné', price: 34.9, quantity: 1, done: false, color: 'bg-amber-100 text-amber-700' },
  { id: 3, name: 'Kuřecí prsa', detail: '500 g · chlazené', price: 89.9, quantity: 1, done: true, color: 'bg-rose-100 text-rose-700' },
  { id: 4, name: 'Toaletní papír', detail: '8 ks · Softy', price: 79.9, quantity: 1, done: false, color: 'bg-violet-100 text-violet-700' },
]

const money = (value: number) => `${value.toLocaleString('cs-CZ', { minimumFractionDigits: 2, maximumFractionDigits: 2 })} Kč`

export default function Page() {
  const [tab, setTab] = useState<Tab>('Domů')
  const [items, setItems] = useState(initialItems)
  const [dark, setDark] = useState(false)
  const [budget, setBudget] = useState(12000)
  const [expenseOpen, setExpenseOpen] = useState(false)
  const [notificationsOpen, setNotificationsOpen] = useState(false)
  const [notifications, setNotifications] = useState([
    { id: 1, title: 'Nová akce v Lidlu', detail: 'Mléko je dnes o 20 % levnější.', unread: true },
    { id: 2, title: 'Rozpočet je pod kontrolou', detail: 'Zbývá vám 4 650 Kč do konce měsíce.', unread: true },
    { id: 3, title: 'Petr doplnil seznam', detail: 'Přidal položku „Ovesné vločky“.', unread: false },
  ])
  const [expenses, setExpenses] = useState([{ id: 1, amount: 7350, note: 'Výdaje domácnosti' }])
  const [newItem, setNewItem] = useState('')
  const spent = expenses.reduce((total, expense) => total + expense.amount, 0)
  const remaining = budget - spent
  const completed = items.filter((item) => item.done).length

  const title = tab === 'Domů' ? 'Dobré ráno, Lucie' : tab
  const subtitle = tab === 'Domů' ? 'Pojďme dnes ušetřit pár korun.' : 'Vše, co potřebujete mít pod kontrolou.'

  function addItem() {
    const name = newItem.trim()
    if (!name) return
    setItems((current) => [...current, { id: Date.now(), name, detail: '1 ks · bez detailu', price: 0, quantity: 1, done: false, color: 'bg-emerald-100 text-emerald-700' }])
    setNewItem('')
  }

  function updateItem(id: number, changes: Partial<Item>) {
    setItems((current) => current.map((item) => item.id === id ? { ...item, ...changes } : item))
  }

  return (
    <div className={dark ? 'dark min-h-screen' : 'min-h-screen'}>
      <div className="min-h-screen bg-background text-foreground transition-colors">
        <div className="mx-auto flex min-h-screen max-w-[1440px]">
          <aside className="hidden w-64 shrink-0 flex-col border-r border-border bg-card px-5 py-7 lg:flex">
            <Brand />
            <nav className="mt-12 space-y-1" aria-label="Hlavní navigace">
              {(['Domů', 'Nákup', 'Obchody', 'Rozpočet', 'AI', 'Profil'] as Tab[]).map((item) => <NavItem key={item} item={item} active={tab === item} onClick={() => setTab(item)} />)}
            </nav>
            <div className="mt-auto rounded-2xl bg-primary p-4 text-primary-foreground">
              <Sparkles className="mb-5 h-5 w-5" />
              <p className="text-sm font-semibold">Chytré nákupy začínají tady.</p>
              <p className="mt-1 text-xs opacity-70">Využijte AI doporučení pro další úsporu.</p>
              <button onClick={() => setTab('AI')} className="mt-4 flex items-center gap-1 text-xs font-semibold">Vyzkoušet AI <ArrowUpRight className="h-3.5 w-3.5" /></button>
            </div>
          </aside>

          <main className="min-w-0 flex-1 pb-24 lg:pb-8">
            <header className="flex items-center justify-between px-5 py-5 sm:px-8 lg:px-12 lg:py-8">
              <div className="flex items-center gap-3 lg:hidden"><Brand compact /></div>
              <div className="hidden lg:block"><p className="text-sm text-muted-foreground">Pátek 19. září 2026</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">{title}</h1></div>
              <div className="ml-auto flex items-center gap-2">
                <button aria-label="Přepnout motiv" onClick={() => setDark(!dark)} className="icon-button">{dark ? <Sun /> : <Moon />}</button>
                <button aria-label="Oznámení" onClick={() => setNotificationsOpen((open) => !open)} className="icon-button relative"><Bell /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-primary" /></button>
                <button className="ml-1 flex h-9 w-9 items-center justify-center rounded-full bg-[#f4b183] text-sm font-semibold text-[#5b321f]">LK</button>
              </div>
            </header>
            {notificationsOpen && <NotificationPanel notifications={notifications} onRead={(id) => setNotifications((current) => current.map((notification) => notification.id === id ? { ...notification, unread: false } : notification))} onClose={() => setNotificationsOpen(false)} />}

            <div className="px-5 sm:px-8 lg:px-12">
              <div className="mb-7 lg:hidden"><p className="text-sm text-muted-foreground">Pátek 19. září 2026</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">{title}</h1><p className="mt-1 text-sm text-muted-foreground">{subtitle}</p></div>
              {tab === 'Domů' && <><Dashboard budget={budget} spent={spent} remaining={remaining} completed={completed} onShopping={() => setTab('Nákup')} onExpense={() => setExpenseOpen(true)} /><QuickActions onShopping={() => setTab('Nákup')} onStores={() => setTab('Obchody')} onAi={() => setTab('AI')} /></>}
              {tab === 'Nákup' && <ShoppingV2 items={items} newItem={newItem} setNewItem={setNewItem} addItem={addItem} updateItem={updateItem} removeItem={(id) => setItems((current) => current.filter((item) => item.id !== id))} toggle={(id) => setItems((current) => current.map((item) => item.id === id ? { ...item, done: !item.done } : item))} />}
              {tab === 'Obchody' && <Stores />}
              {tab === 'Rozpočet' && <Budget budget={budget} setBudget={setBudget} spent={spent} onExpense={() => setExpenseOpen(true)} />}
              {tab === 'AI' && <AI onShopping={() => setTab('Nákup')} />}
              {tab === 'Profil' && <Profile />}
            </div>
          </main>
        </div>
        <nav className="fixed inset-x-0 bottom-0 z-10 border-t border-border bg-card/95 px-2 py-2 backdrop-blur lg:hidden" aria-label="Mobilní navigace"><div className="mx-auto flex max-w-lg justify-around">{(['Domů', 'Nákup', 'Obchody', 'Rozpočet', 'AI', 'Profil'] as Tab[]).map((item) => <NavItem key={item} item={item} active={tab === item} onClick={() => setTab(item)} mobile />)}</div></nav>
        {expenseOpen && <ExpenseModal onClose={() => setExpenseOpen(false)} onSave={(amount, note) => { setExpenses((current) => [...current, { id: Date.now(), amount, note }]); setExpenseOpen(false) }} />}
      </div>
    </div>
  )
}

function Brand({ compact = false }: { compact?: boolean }) { return <div className="flex items-center gap-2"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><ShoppingCart className="h-4 w-4" /></div>{!compact && <span className="text-lg font-semibold tracking-tight">Rodinný nákup</span>}</div> }
function NavItem({ item, active, onClick, mobile = false }: { item: Tab; active: boolean; onClick: () => void; mobile?: boolean }) { const icons = { Domů: Home, Nákup: ListChecks, Obchody: MapPin, Rozpočet: Wallet, AI: Bot, Profil: Users }; const Icon = icons[item]; return <button onClick={onClick} className={`flex items-center gap-3 rounded-xl text-sm font-medium transition-colors ${mobile ? 'min-w-12 flex-col gap-1 px-2 py-1 text-[10px]' : 'w-full px-3 py-2.5'} ${active ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon className={mobile ? 'h-5 w-5' : 'h-[18px] w-[18px]'} />{item}</button> }

function Dashboard({ budget, spent, remaining, completed, onShopping, onExpense }: { budget: number; spent: number; remaining: number; completed: number; onShopping: () => void; onExpense: () => void }) { return <div className="space-y-6"><section className="grid gap-4 xl:grid-cols-[1.3fr_0.7fr]"><div className="rounded-3xl bg-primary p-6 text-primary-foreground sm:p-8"><div className="flex items-start justify-between"><div><p className="text-sm opacity-70">Rozpočet domácnosti</p><p className="mt-3 text-4xl font-semibold tracking-tight">{budget.toLocaleString('cs-CZ')} Kč</p></div><CircleDollarSign className="h-6 w-6 opacity-60" /></div><div className="mt-8 flex items-end justify-between"><div><p className="text-xs opacity-60">Utraceno tento měsíc</p><p className="mt-1 font-medium">{spent.toLocaleString('cs-CZ')} Kč</p></div><div className="text-right"><p className="text-xs opacity-60">Zbývá</p><p className="mt-1 font-medium">{remaining.toLocaleString('cs-CZ')} Kč</p></div></div><div className="mt-3 h-2 rounded-full bg-white/20"><div className="h-full w-[61%] rounded-full bg-[#d7f36b]" /></div></div><button onClick={onShopping} className="group rounded-3xl border border-border bg-card p-6 text-left transition hover:-translate-y-0.5 hover:shadow-lg sm:p-8"><div className="flex items-center justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#d7f36b] text-[#27320d]"><ListChecks className="h-5 w-5" /></div><ChevronRight className="h-5 w-5 text-muted-foreground transition group-hover:translate-x-1" /></div><p className="mt-7 text-sm text-muted-foreground">Týdenní nákup</p><p className="mt-1 text-2xl font-semibold">{completed} z {4} hotovo</p><p className="mt-2 text-xs text-muted-foreground">Ještě zbývá {4 - completed} položky</p></button></section><section className="grid gap-6 xl:grid-cols-[1fr_1fr]"><div className="rounded-3xl border border-border bg-card p-6 sm:p-7"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold">Výdaje podle kategorií</p><p className="mt-1 text-xs text-muted-foreground">Září 2026</p></div><button onClick={() => {}} className="text-xs font-medium text-primary">Podrobnosti</button></div><div className="mt-7 flex items-end gap-2" style={{ height: 150 }}>{[['Bydlení', 78], ['Jídlo', 58], ['Děti', 42], ['Doprava', 30], ['Ostatní', 20]].map(([label, height]) => <div key={label as string} className="flex flex-1 flex-col items-center gap-2"><div className="flex h-[120px] w-full items-end"><div className="w-full rounded-t-lg bg-primary/80" style={{ height: `${height}%` }} /></div><span className="text-[10px] text-muted-foreground">{label}</span></div>)}</div></div><div className="rounded-3xl bg-[#d7f36b] p-6 text-[#27320d] sm:p-7"><div className="flex items-start justify-between"><div><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles className="h-4 w-4" /> Tip od AI</div><p className="mt-5 max-w-sm text-xl font-semibold leading-snug">V Albertu je tento týden kuřecí maso o 20 % levnější.</p><p className="mt-3 text-sm opacity-70">Na vašem seznamu byste mohli ušetřit až 45 Kč.</p></div><button onClick={onShopping} className="rounded-full bg-[#27320d] p-2 text-[#d7f36b]" aria-label="Zobrazit doporučení"><ArrowUpRight className="h-4 w-4" /></button></div><button onClick={onShopping} className="mt-8 text-sm font-semibold underline underline-offset-4">Zobrazit nabídku</button></div></section><section className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-border bg-card px-5 py-4"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-muted"><Wallet className="h-4 w-4" /></div><div><p className="text-sm font-medium">Přidat výdaj</p><p className="text-xs text-muted-foreground">Zapište dnešní nákup</p></div></div><button onClick={onExpense} className="rounded-xl bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">+ Nový výdaj</button></section></div> }

function Shopping({ items, newItem, setNewItem, addItem, updateItem, toggle }: { items: Item[]; newItem: string; setNewItem: (v: string) => void; addItem: () => void; updateItem: (id: number, changes: Partial<Item>) => void; toggle: (id: number) => void }) { const visibleItems = items; return <div className="mx-auto max-w-3xl space-y-5"><div className="flex items-end justify-between"><div><p className="text-sm text-muted-foreground">Sdílený seznam</p><h2 className="mt-1 text-2xl font-semibold">Týdenní nákup</h2></div><button className="flex items-center gap-2 rounded-xl border border-border px-3 py-2 text-sm"><Search className="h-4 w-4" /> Hledat</button></div><div className="rounded-2xl border border-border bg-card p-2"><div className="flex gap-2"><input value={newItem} onChange={(e) => setNewItem(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && addItem()} placeholder="Co potřebujete koupit?" className="min-w-0 flex-1 bg-transparent px-3 py-2 text-sm outline-none" /><button onClick={addItem} className="flex items-center gap-1 rounded-xl bg-primary px-3 py-2 text-sm font-medium text-primary-foreground"><Plus className="h-4 w-4" /> Přidat</button></div></div><div className="overflow-hidden rounded-3xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div className="flex items-center gap-2"><ListChecks className="h-4 w-4 text-primary" /><span className="text-sm font-semibold">{items.filter((i) => !i.done).length} zbývá</span></div><span className="text-xs text-muted-foreground">Odhad {money(items.reduce((sum, i) => sum + i.price * i.quantity, 0))}</span></div>{visibleItems.map((item) => <div key={item.id} className="flex items-center gap-3 border-b border-border px-5 py-4 last:border-0"><button aria-label={item.done ? 'Označit jako nedokončené' : 'Označit jako zakoupené'} onClick={() => toggle(item.id)} className={`flex h-6 w-6 shrink-0 items-center justify-center rounded-full border ${item.done ? 'border-primary bg-primary text-primary-foreground' : 'border-muted-foreground/40'}`}>{item.done && <Check className="h-3.5 w-3.5" />}</button><div className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-xl text-xs font-bold ${item.color}`}>{item.name.slice(0, 2).toUpperCase()}</div><div className="min-w-0 flex-1"><p className={`truncate text-sm font-medium ${item.done ? 'text-muted-foreground line-through' : ''}`}>{item.name}</p><p className="mt-0.5 text-xs text-muted-foreground">{item.detail}</p></div><p className="text-sm font-semibold">{item.price ? money(item.price) : '—'}</p><button aria-label="Další možnosti" className="text-muted-foreground"><ChevronRight className="h-4 w-4" /></button></div>)}</div></div> }

function ShoppingV2({ items, newItem, setNewItem, addItem, updateItem, removeItem, toggle }: { items: Item[]; newItem: string; setNewItem: (v: string) => void; addItem: () => void; updateItem: (id: number, changes: Partial<Item>) => void; removeItem: (id: number) => void; toggle: (id: number) => void }) { const [query, setQuery] = useState(''); const visibleItems = items.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); return <div className="mx-auto max-w-3xl space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm text-muted-foreground">Sdílený seznam</p><h2 className="mt-1 text-2xl font-semibold">Týdenní nákup</h2></div><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-medium text-primary">Demo seznam</span></div><div className="rounded-2xl border border-border bg-card p-2"><div className="flex gap-2"><input aria-label="Nová položka" value={newItem} onChange={(e) => setNewItem(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.nativeEvent.isComposing && e.keyCode !== 229) addItem() }} placeholder="Co potřebujete koupit?" className="min-w-0 flex-1 bg-transparent px-3 py-2 text-sm outline-none" /><button onClick={addItem} className="flex items-center gap-1 rounded-xl bg-primary px-3 py-2 text-sm font-medium text-primary-foreground"><Plus className="h-4 w-4" /> Přidat</button></div></div><label className="flex items-center gap-2 rounded-2xl border border-border bg-card px-4 py-3 text-sm text-muted-foreground"><Search className="h-4 w-4" /><span className="sr-only">Filtrovat seznam</span><input aria-label="Filtrovat seznam" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Filtrovat položky" className="min-w-0 flex-1 bg-transparent text-foreground outline-none" /></label>{items.length === 0 ? <div className="rounded-3xl border border-dashed border-border bg-card p-10 text-center"><ListChecks className="mx-auto h-8 w-8 text-muted-foreground" /><p className="mt-3 font-semibold">Seznam je prázdný</p><p className="mt-1 text-sm text-muted-foreground">Přidejte první položku pomocí formuláře výše.</p></div> : <div className="overflow-hidden rounded-3xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div className="flex items-center gap-2"><ListChecks className="h-4 w-4 text-primary" /><span className="text-sm font-semibold">{items.filter((i) => !i.done).length} zbývá</span></div><span className="text-xs text-muted-foreground">Odhad {money(items.reduce((sum, i) => sum + i.price * i.quantity, 0))}</span></div>{visibleItems.map((item) => <div key={item.id} className="flex items-center gap-3 border-b border-border px-5 py-4 last:border-0"><button aria-label={item.done ? 'Označit jako nedokončené' : 'Označit jako zakoupené'} onClick={() => toggle(item.id)} className={`flex h-6 w-6 shrink-0 items-center justify-center rounded-full border ${item.done ? 'border-primary bg-primary text-primary-foreground' : 'border-input'}`}>{item.done && <Check className="h-4 w-4" />}</button><div className="min-w-0 flex-1"><p className={`font-medium ${item.done ? 'text-muted-foreground line-through' : ''}`}>{item.name}</p><p className="mt-1 text-xs text-muted-foreground">{item.detail}</p><div className="mt-3 flex flex-wrap items-center gap-2"><label className="text-xs text-muted-foreground">Ks<input aria-label={`Množství ${item.name}`} type="number" min="1" value={item.quantity} onChange={(e) => updateItem(item.id, { quantity: Math.max(1, Number(e.target.value) || 1) })} className="ml-1 w-14 rounded-lg border border-input bg-background px-2 py-1 text-xs" /></label><label className="text-xs text-muted-foreground">Cena<input aria-label={`Cena ${item.name}`} type="number" min="0" step="0.1" value={item.price} onChange={(e) => updateItem(item.id, { price: Math.max(0, Number(e.target.value) || 0) })} className="ml-1 w-20 rounded-lg border border-input bg-background px-2 py-1 text-xs" /></label><span className="text-xs font-medium text-primary">{money(item.price * item.quantity)}</span></div></div><button aria-label={`Odstranit ${item.name}`} onClick={() => removeItem(item.id)} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive"><X className="h-4 w-4" /></button></div>)}</div>}</div> }

function Stores() { const [location, setLocation] = useState('Praha 4'); const [query, setQuery] = useState(''); const [selected, setSelected] = useState<string | null>(null); const stores = [['Lidl', '2 akce na vašem seznamu', 'bg-[#d7f36b]'], ['Albert', 'Nejbližší · 1,2 km', 'bg-[#f4b183]'], ['Kaufland', '8 aktivních nabídek', 'bg-[#b9d8f5]'], ['Billa', 'Otevřeno do 21:00', 'bg-[#f3c0d3]'], ['JIP', '4 akce v okolí', 'bg-[#c9b8ef]'], ['Penny', 'Otevřeno do 20:00', 'bg-[#f6d38b]']]; const visibleStores = stores.filter(([name]) => name.toLowerCase().includes(query.toLowerCase())); return <div className="space-y-6"><div><p className="text-sm text-muted-foreground">Obchody ve vašem okolí</p><h2 className="mt-1 text-2xl font-semibold">Kde nakoupit</h2></div><div className="flex flex-wrap gap-2"><label className="flex min-w-0 flex-1 items-center gap-2 rounded-2xl border border-border bg-card px-4 py-3 text-sm text-muted-foreground"><MapPin className="h-4 w-4 shrink-0 text-primary" /><span className="sr-only">Lokalita</span><input value={location} onChange={(event) => setLocation(event.target.value)} className="min-w-0 flex-1 bg-transparent text-foreground outline-none" aria-label="Lokalita" /></label><label className="flex min-w-[180px] flex-1 items-center gap-2 rounded-2xl border border-border bg-card px-4 py-3 text-sm text-muted-foreground"><Search className="h-4 w-4 shrink-0" /><span className="sr-only">Hledat obchod</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Hledat obchod" className="min-w-0 flex-1 bg-transparent outline-none" aria-label="Hledat obchod" /></label></div><div className="grid gap-4 sm:grid-cols-2">{visibleStores.map(([name, detail, color]) => <button key={name} onClick={() => setSelected(name)} className="rounded-3xl border border-border bg-card p-5 text-left transition hover:-translate-y-0.5 hover:shadow-lg"><div className={`flex h-16 items-center justify-center rounded-2xl ${color} text-2xl font-bold text-[#27320d]`}>{name.slice(0, 1)}</div><div className="mt-4 flex items-end justify-between"><div><h3 className="font-semibold">{name}</h3><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div><ChevronRight className="h-4 w-4 text-muted-foreground" /></div></button>)}</div>{visibleStores.length === 0 && <div className="rounded-3xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">Žádný obchod neodpovídá hledání.</div>}{selected && <div role="status" className="rounded-2xl bg-primary/10 p-4 text-sm text-primary">Vybrali jste {selected} pro lokalitu {location}. Zobrazujeme nejlepší akce pro váš seznam.</div>}</div> }

function Budget({ budget, setBudget, spent, onExpense }: { budget: number; setBudget: (v: number) => void; spent: number; onExpense: () => void }) { const categories = [['Bydlení', 4200, 'bg-primary'], ['Potraviny', 3150, 'bg-[#f4b183]'], ['Děti', 1200, 'bg-[#b9d8f5]'], ['Doprava', 800, 'bg-[#d7f36b]']]; return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm text-muted-foreground">Září 2026</p><h2 className="mt-1 text-2xl font-semibold">Rozpočet domácnosti</h2></div><button onClick={onExpense} className="rounded-xl bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">+ Přidat výdaj</button></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Celkem" value={`${budget.toLocaleString('cs-CZ')} Kč`} icon={<Wallet />} /><Stat label="Utraceno" value={`${spent.toLocaleString('cs-CZ')} Kč`} icon={<TrendingDown />} /><Stat label="Zbývá" value={`${(budget - spent).toLocaleString('cs-CZ')} Kč`} icon={<CircleDollarSign />} /></div><div className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><div className="rounded-3xl border border-border bg-card p-6"><div className="flex justify-between"><p className="font-semibold">Rozdělení rozpočtu</p><button onClick={() => setBudget(budget === 12000 ? 14000 : 12000)} className="text-xs text-primary">Upravit limit</button></div><div className="mt-6 space-y-5">{categories.map(([name, value, color]) => <div key={name as string}><div className="mb-2 flex justify-between text-sm"><span>{name}</span><span className="font-medium">{(value as number).toLocaleString('cs-CZ')} Kč</span></div><div className="h-2 rounded-full bg-muted"><div className={`h-full rounded-full ${color}`} style={{ width: `${Math.min((value as number) / budget * 100 * 2.2, 100)}%` }} /></div></div>)}</div></div><div className="rounded-3xl bg-[#f4b183] p-6 text-[#5b321f]"><Tag className="h-5 w-5" /><p className="mt-8 text-2xl font-semibold">Akce vám tento měsíc ušetřily 380 Kč.</p><p className="mt-3 text-sm opacity-70">Nejvíce na potravinách a drogerii.</p></div></div></div> }
function QuickActions({ onShopping, onStores, onAi }: { onShopping: () => void; onStores: () => void; onAi: () => void }) { const actions = [{ label: 'Přidat do nákupu', detail: 'Doplňte sdílený seznam', icon: Plus, onClick: onShopping }, { label: 'Najít akce', detail: 'Porovnejte obchody poblíž', icon: Tag, onClick: onStores }, { label: 'Zeptat se AI', detail: 'Naplánujte úsporný nákup', icon: Sparkles, onClick: onAi }]; return <section className="grid gap-3 sm:grid-cols-3" aria-label="Rychlé akce">{actions.map(({ label, detail, icon: Icon, onClick }) => <button key={label} onClick={onClick} className="group flex items-center gap-3 rounded-2xl border border-border bg-card p-4 text-left transition hover:-translate-y-0.5 hover:shadow-md"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon /></span><span className="min-w-0"><span className="block text-sm font-semibold">{label}</span><span className="mt-0.5 block truncate text-xs text-muted-foreground">{detail}</span></span><ChevronRight className="ml-auto shrink-0 text-muted-foreground transition group-hover:translate-x-0.5" /></button>)}</section> }

function Stat({ label, value, icon }: { label: string; value: string; icon: React.ReactNode }) { return <div className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs">{label}</span>{icon}</div><p className="mt-4 text-xl font-semibold">{value}</p></div> }
function AI({ onShopping }: { onShopping: () => void }) { const [prompt, setPrompt] = useState(''); const [answer, setAnswer] = useState(''); function suggest() { if (!prompt.trim()) return; setAnswer('Doporučuji týdenní nákup do 2 500 Kč. Přidám základní potraviny, porovnám akce a nechám rezervu 380 Kč.'); } return <div className="mx-auto max-w-3xl space-y-6"><div className="rounded-3xl bg-primary p-6 text-primary-foreground sm:p-8"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-[#d7f36b] text-[#27320d]"><Bot /></div><h2 className="mt-6 text-3xl font-semibold tracking-tight">Váš nákupní parťák.</h2><p className="mt-3 max-w-lg text-sm leading-relaxed opacity-70">Řekněte mi, co potřebujete. Pomůžu vám sestavit nákup, najít akce a uhlídat rozpočet.</p><div className="mt-7 flex gap-2 rounded-2xl bg-white/10 p-2"><input value={prompt} onChange={(e) => setPrompt(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.nativeEvent.isComposing && e.keyCode !== 229) suggest() }} placeholder="Např. nákup na týden do 2 500 Kč" className="min-w-0 flex-1 bg-transparent px-3 py-2 text-sm text-white outline-none placeholder:text-white/50" /><button onClick={suggest} className="rounded-xl bg-[#d7f36b] px-4 py-2 text-sm font-semibold text-[#27320d]">Navrhnout</button></div>{answer && <div className="mt-4 rounded-2xl bg-white/10 p-4 text-sm leading-relaxed"><Sparkles className="mb-2 h-4 w-4 text-[#d7f36b]" />{answer}<button onClick={onShopping} className="mt-3 block font-semibold text-[#d7f36b]">Otevřít nákupní seznam →</button></div>}</div><div className="rounded-3xl border border-border bg-card p-6"><p className="text-sm font-semibold">Co umí váš asistent?</p><div className="mt-5 grid gap-3 sm:grid-cols-3">{[['Najde akce', 'Porovná ceny v obchodech'], ['Pohlídá rozpočet', 'Udrží nákup pod limitem'], ['Naplánuje nákup', 'Rozdělí položky chytře']].map(([a, b]) => <div key={a} className="rounded-2xl bg-muted p-4"><Sparkles className="h-4 w-4 text-primary" /><p className="mt-4 text-sm font-medium">{a}</p><p className="mt-1 text-xs text-muted-foreground">{b}</p></div>)}</div></div></div> }
function Profile() { const [name, setName] = useState('Lucie Králová'); const [household, setHousehold] = useState('Královi'); const [members, setMembers] = useState(4); const [saved, setSaved] = useState(false); return <div className="mx-auto max-w-2xl space-y-6"><div><p className="text-sm text-muted-foreground">Nastavení</p><h2 className="mt-1 text-2xl font-semibold">Můj profil</h2></div><div className="rounded-3xl border border-border bg-card p-6"><div className="flex items-center gap-4"><div className="flex size-16 items-center justify-center rounded-full bg-[#f4b183] text-lg font-semibold text-[#5b321f]">LK</div><div><h3 className="font-semibold">{name}</h3><p className="text-sm text-muted-foreground">lucie@example.cz</p></div></div><div className="mt-8 flex flex-col gap-4"><label className="text-sm">Jméno<input aria-label="Jméno" value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none focus:ring-2 focus:ring-ring" /></label><label className="text-sm">Název domácnosti<input aria-label="Název domácnosti" value={household} onChange={(event) => setHousehold(event.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none focus:ring-2 focus:ring-ring" /></label><label className="text-sm">Členové domácnosti<select aria-label="Členové domácnosti" value={members} onChange={(event) => setMembers(Number(event.target.value))} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none focus:ring-2 focus:ring-ring"><option value={1}>1 osoba</option><option value={2}>2 osoby</option><option value={3}>3 osoby</option><option value={4}>4 osoby</option><option value={5}>5 osob</option></select></label><div className="flex items-center justify-between border-t border-border pt-4 text-sm"><span>Výchozí měna</span><span className="text-muted-foreground">CZK</span></div><button onClick={() => setSaved(true)} className="rounded-xl bg-primary px-4 py-3 text-sm font-medium text-primary-foreground">Uložit změny</button>{saved && <p role="status" className="text-sm text-primary">Změny profilu byly uloženy.</p>}</div></div></div> }
function NotificationPanel({ notifications, onRead, onClose }: { notifications: { id: number; title: string; detail: string; unread: boolean }[]; onRead: (id: number) => void; onClose: () => void }) {
  return <div className="absolute right-4 top-16 z-20 w-[min(calc(100vw-2rem),360px)] rounded-2xl border border-border bg-card p-3 shadow-xl sm:right-8 lg:right-12">
    <div className="flex items-center justify-between px-2 py-2"><div><p className="text-sm font-semibold">Upozornění</p><p className="text-xs text-muted-foreground">Aktuality pro vaši domácnost</p></div><button onClick={onClose} className="icon-button" aria-label="Zavřít upozornění"><X /></button></div>
    <div className="mt-2 flex flex-col gap-1">{notifications.map((notification) => <button key={notification.id} onClick={() => onRead(notification.id)} className="flex gap-3 rounded-xl p-3 text-left transition hover:bg-muted"><span className={`mt-1 size-2 shrink-0 rounded-full ${notification.unread ? 'bg-primary' : 'bg-muted-foreground/30'}`} /><span className="min-w-0"><span className="block text-sm font-medium">{notification.title}</span><span className="mt-0.5 block text-xs leading-relaxed text-muted-foreground">{notification.detail}</span></span></button>)}</div>
  </div>
}

function ExpenseModal({ onClose, onSave }: { onClose: () => void; onSave: (amount: number, note: string) => void }) { const [amount, setAmount] = useState(''); const [note, setNote] = useState(''); const save = () => { const value = Number(amount.replace(',', '.')); if (!Number.isFinite(value) || value <= 0) return; onSave(value, note.trim() || 'Nový výdaj') }; return <div className="fixed inset-0 z-20 flex items-end justify-center bg-black/40 p-0 sm:items-center sm:p-4"><div className="w-full max-w-md rounded-t-3xl bg-card p-6 shadow-2xl sm:rounded-3xl"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Nový výdaj</h2><button onClick={onClose} aria-label="Zavřít" className="icon-button"><X /></button></div><div className="mt-6 space-y-4"><label className="block text-sm">Částka<input autoFocus value={amount} onChange={(event) => setAmount(event.target.value)} type="text" inputMode="decimal" placeholder="0,00 Kč" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none focus:ring-2 focus:ring-ring" /></label><label className="block text-sm">Poznámka<input value={note} onChange={(event) => setNote(event.target.value)} placeholder="Např. nákup v Lidlu" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none focus:ring-2 focus:ring-ring" /></label><button onClick={save} className="w-full rounded-xl bg-primary py-3 text-sm font-medium text-primary-foreground">Uložit výdaj</button></div></div></div> }
